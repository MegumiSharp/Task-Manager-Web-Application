# Task-Manager-Web-Application
Small full-stack web application built for a technical internship challenge. The app manages tasks (CRUD), a virtual wallet with business rules, and an audit log of all actions. It includes a SPA frontend, a FastAPI backend, SQLite persistence, and a clean UI with dark mode and filters.
